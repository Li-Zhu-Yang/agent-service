# ---- Web 框架 ----
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9

# ---- 配置 / 数据 ----
pydantic>=2.7
pydantic-settings>=2.3
SQLAlchemy>=2.0.30
alembic>=1.13
psycopg[binary]>=3.1.19        # PostgreSQL 驱动（psycopg3，兼容新版本 Python）
redis>=5.0

# ---- LLM / Embedding ----
openai>=1.40

# ---- Agent ----
langgraph>=0.2.50
langchain-core>=0.3.0

# ---- 向量库 / 检索 ----
chromadb>=0.5.0
rank-bm25>=0.2.2

# ---- 文档解析 ----
pypdf>=4.2
python-docx>=1.1

# ---- 认证 / 工具 ----
PyJWT>=2.8
httpx>=0.27

# ---- MCP ----
# FastMCP 为 mcp 1.x API；2.x 已改名 MCPServer 且 API 变动较大，暂锁定 1.x
mcp>=1.2,<2

# ---- 测试 ----
pytest>=8.2
pytest-asyncio>=0.23
